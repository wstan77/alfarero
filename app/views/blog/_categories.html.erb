<div class="widget categories">
    <h3>Categorías</h3>
    <div class="row">
        <div class="col-sm-12">
            <ul class="blog_category">
                <% @categories.each do |c| %>
                    <li><%= link_to "#{c.title} <span class='badge'>#{c.blog_entries_count}</span>".html_safe, blog_category_path(c)%></li> 
                <% end %> 
            </ul>
        </div>
    </div>                     
</div><!--/.categories-->