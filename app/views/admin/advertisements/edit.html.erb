<%= simple_form_for(@admin_advertisement, url: admin_advertisement_path, method: :patch, html: {multipart: true}) do |f| %>
	<%= render partial: 'form', locals: {:f => f}%>
 
 	<div class="form-actions">
    	<%= f.submit "Actualizar registro", class: "btn btn-primary" %> | <%= link_to '<< Cancelar', admin_advertisements_path, class: "btn btn-default" %>
  	</div> 
<% end %>


